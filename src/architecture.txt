So each element has some number of nodes related to its function
For simplification for now let's only deal with elements with 2 nodes

We call the element the parent
The nodes are the children
If one element has multiple nodes, then those nodes are siblings
Each node is assigned a number
If a node has the number n, then there also exists a node with number -n
This is considering the node's primary sibling, and all other nodes are secondary siblings

The direction of the component is determined by which node appears first.
If a negative node apprears first, then that element is backwards.

Simulation Loop:

1. Find all loops
2. Update loop list
3. Simulate time step

For inverted elements, the current must be transformed, as well as the voltage.

So for a resistor, if it's positively oriented, then
V_proper = (current * 1) * resistance
V_actual = V_proper * 1

But if it's negatively oriented,
V_proper = (current * -1) * resistance
V_actual = V_proper * -1

Likewise for a positively oriented capacitor
V_proper = (stored_charge + current * 1 * dt) / capacitance
V_actual = V_proper * 1

And for a negatively oriented capacitor
V_proper = (stored_charge + current * -1 * dt) / capacitance
V_actual = V_proper * -1

All of this is linear tho so it literally should make no difference at all what am I doing!
Oh well I suppose it matters only for the capacitor and battery where direction has some meaning
And also it mediates the interaction between circuits that might not flow in the same direction
So actually this its probably fine